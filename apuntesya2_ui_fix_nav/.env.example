# Rename to .env for local dev OR add these as Environment Variables in Render.
FLASK_ENV=production
SECRET_KEY=change-me-please
# Use Postgres in Render for persistence (recommended). Example provided by the Render Postgres add-on.
DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname
# Or for quick tests only (ephemeral on Render Free):
# DATABASE_URL=sqlite:///app.db

# Mercado Pago (ajusta con tus credenciales)
MP_PUBLIC_KEY=
MP_ACCESS_TOKEN=
MP_WEBHOOK_SECRET=

# Comisiones (ajustá según tu versión estable)
PLATFORM_COMMISSION=0.05
MP_COMMISSION_RATE=0.064
